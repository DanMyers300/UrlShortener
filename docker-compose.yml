services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - db-data:/app/data
    environment:
      - ConnectionStrings__DefaultConnection=Data Source=/app/data/urlshortener.db

volumes:
  db-data:
